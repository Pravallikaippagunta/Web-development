const boxElement = document.getElementById("box");
boxElement.addEventListener("mouseover", function () {
  alert(
    "you have entered in Restricted Area (mouse over event is working properly)"
  );
});